import pytest

# Disabled: This legacy file referenced removed NMR models/API. Skipping to avoid collection errors.
pytest.skip("Disabled legacy yield calc tests (NMR-based)", allow_module_level=True)
